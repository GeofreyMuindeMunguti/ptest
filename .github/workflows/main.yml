name: Build Docker Image

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  ci:
    runs-on: ubuntu-latest
    container:
      image: node

    steps:
      - uses: actions/checkout@v1
      - name: Install
        run: |
          npm install
  cd:
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - uses: actions/checkout@v1
      - name: Docker login
        run: docker login -u muinde -p geofrey7543
      - name: Build
        run: docker build -t user-service .
      - name: Tags
        run: |
          docker tag user-service muinde/user-service:latest
      - name: Push
        run: |
          docker push muinde/user-service:latest
      - name: Deploy
        uses: appleboy/ssh-action@master
        with:
          host: 3.16.36.242
          username: ubuntu
          password: godigital2020
          port: 22
          script: |
            sudo docker stop $(sudo docker ps -a -q)
            sudo docker login -u muinde -p geofrey7543
            sudo docker pull muinde/user-service:latest
            sudo docker run -dit -p 8001:8001 muinde/user-service:latest
